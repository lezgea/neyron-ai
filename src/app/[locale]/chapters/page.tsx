import ChaptersList from 'src/app/[locale]/components/core/ChaptersList';


const page = () => {
    function test() {
        const aaarb = 'sdsd'
    }
    
    return (
        <main>
            <ChaptersList mainPage={false} />
        </main>
    );
};

export default page;
