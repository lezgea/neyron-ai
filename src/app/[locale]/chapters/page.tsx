import ChaptersList from "../components/core/chapters-list";


const page = () => {
    function test() {
        const aaarb = 'sdsd'
    }

    return (
        <main>
            <ChaptersList mainPage={false} />
        </main>
    );
};

export default page;
