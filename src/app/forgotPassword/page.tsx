import React from 'react';

import DetectToken from './detectToken';

const Login = () => {
  return (
    <section id="login" className="container">
      <DetectToken />
    </section>
  );
};

export default Login;
